# 新舊系統人工比對報告

比對日期：2026-07-27（Asia/Taipei）  
比對者：Codex

## 比對範圍與限制

- 舊系統：`/Users/jay/Documents/IntelliJProjects/onlinebanking`，僅讀取其畫面、Flow、Service、DAO、Entity 與執行頁面。
- 新系統：`DXC2-OnlineBankingSystem`，使用目前本機 Oracle、Spring Boot 與 Vue 執行環境。
- 為避免改變舊系統資料，沒有在舊 Oracle 寫入測試使用者、帳戶或交易；新系統的輸入／輸出則以先前各功能切片的 Oracle REST 實測結果作為同值比對資料。新 Oracle 當時已有使用者 `hank` 的 2 個帳戶與 2 筆交易，本次只讀取，未修改或刪除。

## 執行環境確認

| 項目 | 舊系統 | 新系統 | 結果 |
| --- | --- | --- | --- |
| Oracle | `onlinebanking-oracle`，host port `1521` | `dxc2-oracle-1`，host port `12521` | 兩套獨立容器與 volume |
| Backend | Tomcat，port `8080` | Spring Boot，port `28080` | 服務正常 |
| Frontend | `/onlinebanking/app/account` redirect 至登入頁 | Vite，port `5273` | 入口頁正常 |
| 未登入頁 | `Online Banking Sign In`，HTTP `200` | Vue 首頁 HTTP `200`，route guard 導向登入 | 行為一致 |

## 功能比對結果

| 功能 | 舊系統基準 | 新系統實際結果 | 結論 |
| --- | --- | --- | --- |
| 註冊／登入／登出 | `signUp.xhtml`、`login.xhtml`、`account-flow.xml`，成功後進入帳戶首頁 | REST session、註冊自動登入、登入／登出與錯誤密碼驗證已實測 | 通過 |
| 帳戶 | `newAccount.xhtml`、Account Service，Checking／Saving、4 位數 PIN、初始餘額 0 | `/api/accounts` 與 Vue 建立帳戶已實測，PIN 以 BCrypt 儲存 | 通過 |
| 存款 | `deposit.xhtml`，`0.01–50000.00`，正確 PIN 後增加餘額 | `/api/transactions/deposits` 回傳 `201`，建立 Deposit 紀錄並增加餘額 | 通過 |
| 提款 | `withdraw.xhtml`，`0.01–2000.00`，不可超過餘額 | `/api/transactions/withdrawals` 回傳 `201`，餘額不足、錯誤 PIN 與超額均拒絕 | 通過 |
| 轉帳 | `transfer.xhtml`，目的帳戶可為其他使用者，來源 PIN／餘額驗證 | `/api/transactions/transfers` 跨使用者轉帳成功，來源／目的餘額及 Transfer 紀錄一致 | 通過 |
| 交易紀錄 | `tranHistory.xhtml`／`tranHistory2.xhtml`，帳戶與起訖日期，顯示交易 Note | `/api/transactions/history` 與 Vue 表格顯示 Deposit、Withdraw、Send／Receive Note | 通過 |
| 個人資料 | `userInfo.xhtml`／`userInfoUpdated.xhtml`，編輯本人資料並顯示成功摘要 | `/api/user/profile` GET／PUT 與 Vue 編輯／成功畫面已實測 | 通過 |

## 錯誤與權限比對

- 未登入存取受保護 API：新系統回傳 `401`，與舊系統導向登入流程一致。
- 他人帳戶操作：新系統回傳 `404`，不洩漏帳戶歸屬。
- 錯誤 PIN、餘額不足、同帳戶轉帳及超過金額上限：新系統回傳 `400`，且資料不異動。
- 個人資料非法姓名、Gender、Email、Phone：新系統回傳 `400` 並提供欄位錯誤。
- 交易日期倒置：新系統回傳 `400`；日期範圍包含結束日。

## 畫面比對與已知差異

- Vue 已沿用舊專案 `general.xhtml`、各功能 XHTML、`screen.css` 與必要圖片資源；欄位順序、英文文案、綠色 fieldset、按鈕配置及成功摘要維持一致。
- 品牌名稱由舊版 `Green's Online Banking System` 統一改為需求指定的 `DXC2 Online Banking System`，屬刻意的品牌差異。
- JSF／PrimeFaces inplace editor、calendar、dataTable 與 Web Flow state 改為 Vue route、HTML form、REST API 與元件狀態，屬技術實作差異。
- 舊系統使用 port `8080` 與 `/onlinebanking` context path；新系統使用 Vite `5273`、Spring Boot `28080` 及 `/api` REST path，屬部署差異。
- 舊系統未登入入口使用 server redirect；新系統由 Vue Router guard 處理，使用者可見結果一致。

## 本機運行結論

核心功能的畫面、資料、驗證與權限行為已完成比對；Oracle、Spring Boot 與 Vue 可在本機獨立運行。新資料庫既有使用者 `hank`、帳戶與交易資料已保留，未在本次比對中修改。
