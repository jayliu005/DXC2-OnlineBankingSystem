# DXC2-OnlineBankingSystem 專案編譯與歷程

本檔案記錄 DXC2-OnlineBankingSystem 的重要操作。所有操作均以 Asia/Taipei 時間記錄，不記錄密碼、Token、連線字串或其他機密資訊。

## 專案隔離規則

- `onlinebanking` 與 `DXC2-OnlineBankingSystem` 僅供唯讀參考或並行運行，不修改其程式碼與資料。
- DXC2 使用獨立 Git repository、Oracle container、schema、volume、network 與本機連接埠。
- DXC2 本機連接埠：Oracle `12521`、Spring Boot `28080`、Vue `5273`。

## 操作歷程

| 時間（Asia/Taipei） | 執行者 | 動作 | 涉及項目 | 結果 |
| --- | --- | --- | --- | --- |
| 2026-08-25 | Codex | 建立 DXC2 專案初始骨架 | `DXC2-OnlineBankingSystem`、Git、Vue、Spring Boot、Flyway、Docker Compose | 由已驗證的 DXC2 規格建立獨立副本；排除 `.git`、`target`、`node_modules`、`dist`、`.env` 與 IDE 產物。尚未啟動 DXC2 服務，未影響前兩套系統。 |
| 2026-08-25 12:48:57 CST | Codex | 建置 DXC2 後端 | Java 21、Maven、`backend/pom.xml` | `mvn -Dmaven.repo.local=.m2 clean verify` 成功；35 個 Java source 編譯完成，測試通過並產生 DXC2 Jar。首次編譯的主類別檔名錯誤已修正。 |
| 2026-08-25 12:49:00 CST | Codex | 安裝並驗證 DXC2 前端 | Node.js、npm、Vue、Vite | `npm install` 完成；`npm run format`、`npm run lint`、`npm run build` 全數成功。npm audit 回報 3 個相依套件漏洞，未自動升級版本。 |
| 2026-08-25 12:50:41 CST | Codex | 啟動 DXC2 完整本機系統 | Docker Compose Oracle、Flyway、Spring Boot、Vue | 建立獨立 network、volume `dxc2_onlinebanking_oracle_data` 與 Oracle container；schema `DXC2ONLINEBANKING` 從空白狀態成功套用 V001–V004。Spring Boot 使用 `28080`，Vue 使用 `5273`，未影響前兩套系統。 |
| 2026-08-25 12:51:13 CST | Codex | 驗證三套系統並行運作 | DXC2 `12521/28080/5273`、DXC2 `11521/18080/5173`、legacy `1521/8080` | DXC2 Oracle healthy、Vue HTTP 200、Spring Boot health HTTP 200、未登入 session HTTP 401；前兩套服務仍維持原連接埠運行。DXC2 schema 目前有 `ACCOUNT`、`BANK_USER`、`TRANSACTION_REC`、`flyway_schema_history` 共 4 張表。 |
| 2026-08-25 12:54:10 CST | Codex | 重新啟動需求之服務狀態確認 | DXC2 `12521/28080/5273`、DXC2 `11521/18080/5173`、legacy `1521/8080` | DXC2 已在運行，因此未重啟；Oracle container healthy、Vue HTTP 200、Spring Boot health HTTP 200、未登入 session HTTP 401。Legacy 與 DXC2 端點均維持 HTTP 200，確認未受影響。 |
| 2026-08-25 12:58:31 CST | Codex | 將應用程式識別字串統一為 DXC2 並重新啟動 | backend package／Java package／啟動類別、frontend package／頁面標題、Compose project name | 應用程式名稱改為 `dxc2`，Java package 改為 `com.dxc.dxc2`，啟動類別改為 `Dxc2Application`，Compose project 改為 `dxc2`；保留 Oracle schema、資料表、API path、既有 volume 與功能文案，避免資料或行為變更。 |
| 2026-08-25 12:58:31 CST | Codex | 驗證 DXC2 重新啟動及三套系統隔離 | DXC2 `12521/28080/5273`、DXC2 `11521/18080/5173`、legacy `1521/8080` | DXC2 Oracle `dxc2-oracle-1` healthy、Vue HTTP 200、Spring Boot health HTTP 200、未登入 session HTTP 401；前兩套系統端點均 HTTP 200，未受影響。原 DXC2 volume 保留未刪除。 |
| 2026-08-25 13:08:11 CST | Codex | 依需求確認 DXC2 啟動狀態 | DXC2 `12521/28080/5273` 與既有兩套系統端點 | DXC2 已在運行，Oracle healthy、Vue HTTP 200、Spring Boot health HTTP 200、未登入 session HTTP 401；未重啟服務，legacy 與 DXC2 均維持 HTTP 200。 |
| 2026-08-25 13:12:40 CST | Codex | 將專案文字檔中的精確大寫識別字串統一為 `DXC2` 並重新啟動 | DXC2 文件、前端標題與介面文案；backend；frontend；DXC2 Oracle | 已排除 `.git`、Maven/npm 快取、建置產物與既有 `DXC2` 字串；後端 `clean verify` 成功且 10 項測試全數通過，前端 format、lint、型別檢查與 build 全數成功。DXC2 Oracle healthy、Vue HTTP 200、Spring Boot health HTTP 200、未登入 session HTTP 401；既有服務均 HTTP 200，未受影響。 |
